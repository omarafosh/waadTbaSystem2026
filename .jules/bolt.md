
## 2026-06-23 - Resolved UnifiedMemberService VIP fallback logic memory bottleneck
**Learning:** Found an in-memory filtering bottleneck in `UnifiedMemberService.createPrincipalMember` handling fallback assignment for VIP members (`organizationRepository.findAll().stream().filter(...)`). Found that `OrganizationRepository` natively supports `findFirstByType(OrganizationType)`. Furthermore, when validating service-layer changes using existing unit tests (e.g., `UnifiedMemberServiceTest.java`), tests may be broken initially if newly injected dependencies (like `AuthorizationService` and `MemberWorkflowHistoryRepository`) are not correctly mocked in the test class. Additionally, blindly changing assertions to fit the current code without carefully tracking *why* the test expects a specific string can result in test breakage; the test string had the sentence `" Use createDependentMember() for dependents."` correctly expecting `BusinessRuleException` behavior for dependents.
**Action:** Before optimizing and submitting, ensure that `mvn test` works locally for the targeted class. Always be mindful of mock setups required for service tests, and always read existing assertions thoroughly to avoid mistakenly regressing test definitions. Avoid polluting the Git index with `.orig`, `.rej`, or patch diff files, taking care to `rm` them or using `replace_with_git_merge_diff` instead of `patch`.
